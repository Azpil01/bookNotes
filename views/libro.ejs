<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Noto+Sans+Display:ital,wght@0,100..900;1,100..900&family=Quicksand:wght@300..700&display=swap"
        rel="stylesheet">
    <title>
        <%= book.title %>
    </title>
    <link rel="stylesheet" href="/book.css">
</head>

<body>
    <section class="left-panel">
        <div class="portada">
            <img src="<%= book.cover_url %>" alt="">
        </div>
        <div class="rating">
            <% if (book.score === 5) { %>
                <p>⭐⭐⭐⭐⭐</p>
            <%} else if (book.score === 4) { %>
                <p>⭐⭐⭐⭐</p>
            <%} else if (book.score === 3) { %>
                <p>⭐⭐⭐</p>
            <%}%>
        </div>
        <div class="info-basica">
            <p>ISBN: <%= book.isbn %>
            </p>
            <P>Año de lectura: <%= book.the_year %>
            </P>
        </div>
    </section>

    <section class="main-panel">
        <article class="header">
            <h2><%= book.title%></h2>
            <h4>por <%= book.author %></h4>
        </article>
        <article class="resumen">
            <h4>RESUMEN</h4>
            <p><%= book.user_resume %></p>
        </article>
        <article class="notas">
            <h4>MIS NOTAS PERSONALES</h4>
            <p> <%= book.personal_notes %></p>
        </article>
    </section>

    <section class="right-panel">

    </section>



</body>

</html>